# name: Build and Push LuxeCart Frontend-v2 (Next.js)

# on:
#   push:
#     branches: [main]
#     paths:
#       - "frontend-v2/**"
#       - ".github/workflows/frontend-v2-build.yml"
#   pull_request:
#     paths:
#       - "frontend-v2/**"

# env:
#   IMAGE_NAMESPACE: princewillopah2
#   IMAGE_NAME: luxecart-frontend-v2

# jobs:
#   ci:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: frontend-v2
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#           cache-dependency-path: frontend-v2/package-lock.json

#       - name: Install
#         run: npm ci || npm install

#       - name: Typecheck
#         run: npm run typecheck

#       - name: Lint
#         run: npm run lint --if-present

#       - name: Build
#         run: npm run build

#   build-and-push:
#     needs: ci
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#     steps:
#       - uses: actions/checkout@v4

#       - uses: docker/setup-buildx-action@v3

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_NAME }}
#           tags: |
#             type=raw,value=latest
#             type=sha,prefix=sha-
#             type=ref,event=branch
#             type=ref,event=tag

#       - name: Build & push
#         uses: docker/build-push-action@v5
#         with:
#           context: ./frontend-v2
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           provenance: true
#           sbom: true
#           build-args: |
#             NEXT_PUBLIC_API_URL=https://api.luxecart.example.com

#       - name: Trivy scan (non-blocking)
#         uses: aquasecurity/trivy-action@master
#         continue-on-error: true
#         with:
#           image-ref: ${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_NAME }}:latest
#           format: table
#           severity: CRITICAL,HIGH
#           ignore-unfixed: true
